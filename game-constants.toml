# Shared game constants between client and server
# This is the single source of truth - edit only this file

[physics]
thrust_accel = 500.0      # pixels/sec²
rotation_speed = 3.0      # radians/sec
max_speed = 1000.0        # pixels/sec
drag = 0.98               # velocity multiplier per frame
world_limit = 800.0       # world boundary for wrapping

[gameplay]
ship_radius = 25.0        # pixels
